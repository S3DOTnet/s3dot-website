---
name: safe-publish
description: S3DOTウェブサイトの変更をコミットする前にgit status/diff/lint/buildを確認し、問題なければコミット案を提示する。pushはユーザーの明示的な許可があるまで行わない。「コミットして」「publishして」等のリクエストで使用する。
---

# Safe Publish

## 目的
コミット・push前の安全確認を徹底し、壊れたコードや意図しない変更を本番に持ち込まない。

## 手順(順番厳守)
1. `git status` で変更・未追跡ファイルを確認する
2. `git diff` / `git diff --staged` で差分内容を確認する(check-changes Skillの観点も参照)
3. `npm run lint` を実行し、エラー・警告がないか確認する
4. `npm run build` を実行し、ビルドが成功するか確認する
5. 上記いずれかで問題が見つかった場合は、コミットせずに問題点を日本語で報告する
6. すべて問題なければ、以下を提示する
   - ステージすべきファイル一覧
   - 簡潔な日本語のコミットメッセージ案
   - コミット後に `git status` で確認する旨

## 禁止事項
- ユーザーの明示的な許可なしに `git push` を実行しない
- `.env*` や、APIキー・秘密鍵らしき値を含むファイルをステージしない(含まれている場合は警告して除外を提案する)
- lintまたはbuildが失敗した状態でコミットしない
- ユーザーの承認なしに `--no-verify` 等でチェックをスキップしない

## 完了後
- コミットが完了したら `git status` を実行しクリーンであることを確認する
- push可否をユーザーに尋ね、許可された場合のみ `git push` を行う
